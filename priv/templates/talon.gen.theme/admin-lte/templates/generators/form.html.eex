.box-header.with-border
  .box-title = <%%= @talon_resource %>.form_card_title(@resource)
= form_for @changeset, @action, [class: "form-horizontal"], fn f ->
  .box-body
    - a = {f, @talon}
    <%%= for name <- @talon_resource.display_schema_columns(:form) do %>
      .form-group
        = label f, <%%= ":#{name}" %>, class: "col-sm-2 control-label"
        .col-sm-10
          = Talon.Form.input_builder a, <%%= ":#{name}" %>, class: "form-control", place_holder: "<%%= name %>"
          = error_tag f, <%%= ":#{name}" %>
    <%% end %>
    .box-footer
      = submit "Submit", class: "btn btn-primary"
