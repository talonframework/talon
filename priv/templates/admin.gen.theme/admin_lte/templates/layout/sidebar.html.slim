aside.main-sidebar
  section.sidebar style="height: auto;"
    ul.sidebar-menu
      li.header.text-uppercase Resources
      = if @admin_resource do
        = for resource_path <- @admin_resource.resource_paths(@conn.assigns.ex_admin) do
          = render "nav_resource_link.html", conn: @conn, resource_path: resource_path
      li.header.text-uppercase Actions
      = if @admin_resource do
        = for action_path <- @admin_resource.nav_action_links(Phoenix.Controller.action_name(@conn), @conn.assigns.resource) do
          = render "nav_action_link.html", conn: @conn, action_path: action_path
